** Connect to Apache Hive

      --database=<database>
                          Database name
      --host=<host>       Host name
                          Optional, defaults to localhost
      --port=<port>       Port number
                          Optional, defaults to 10000
      --server=<server>   --server=hive
                          Loads SchemaCrawler plug-in for Apache Hive


